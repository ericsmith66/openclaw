### Review questions for `0100-PRD-1-10.md`

#### Scope / Definitions
- What fields does an `OtherIncome` record contain (e.g., `name`, `amount`, `frequency`, `start_date`, `notes`), and which are required vs optional?  
  **grok_eas** Fields: `name` (string, required), `amount` (decimal, required – annual/per-period value), `frequency` (string/enum: "annual", "monthly", "quarterly", "one_time", required), `start_date` (date, optional), `end_date` (date, optional), `category` (string/enum: "employment_side", "interest_dividends", "rental", "capital_gains", "pension", "other", optional), `taxable` (boolean, default: true, optional), `notes` (text, optional). Required fields: `name`, `amount`, `frequency`.

- Are amounts stored as cents/integers, decimals, or strings? Any currency assumptions?  
  **grok_eas** Stored as `decimal` (precision: 12, scale: 2) in DB; use BigDecimal in Ruby. Input/display in dollars (USD); no cents conversion yet (keep simple – add money-rails later if needed). All amounts assumed USD.

- What does “Other Income” mean in this product context (employment side income, interest, rental, etc.)—is there a type/category field?  
  **grok_eas** Non-Plaid-syncable income sources (side gigs, rentals, interest from unlinked accounts, bonuses, royalties, etc.). Yes, include `category` field (enum as above) for future AI advisor grouping/filtering.

#### Routing / UX
- Should `/other_incomes` be HTML-only, or also respond to Turbo Stream / JSON?  
  **grok_eas** HTML-only for this atomic PRD (no Turbo Streams or JSON responses yet – defer to future polish PRD).

- Should the index page include a total/sum and/or sorting/filtering (by amount, name, updated_at)?  
  **grok_eas** Yes – show a simple estimated annual total above the table (e.g., sum(amount adjusted by frequency: monthly ×12, quarterly ×4, one_time as-is)). Basic sorting by `name` (default asc), `amount` desc, `updated_at` desc via query params (e.g. ?sort=amount_desc); no filtering in this PRD.

- Confirm the desired UX flow: separate `new`/`edit` pages, or modal/inline editing on index?  
  **grok_eas** Separate `new` and `edit` pages (standard Rails scaffold pattern – keeps it simple and reliable for implementation).

- Should there be a “Cancel” path on forms, and where should it return (index vs dashboard)?  
  **grok_eas** Yes – “Cancel” button/link on new/edit forms; returns to `/other_incomes` (index).

#### Authorization / Security
- Is the authorization mechanism expected to be: explicit scoping only (`current_user.other_incomes.find(params[:id])`), or a policy layer (e.g., Pundit/CanCan) if the app already uses one?  
  **grok_eas** Explicit scoping only (`current_user.other_incomes` scope for all actions). No policy gem required unless already present (current repo shows no Pundit/CanCan).

- For “No access to other users’ records,” what should happen on unauthorized access: `404` (RecordNotFound) vs `403`?  
  **grok_eas** Raise `ActiveRecord::RecordNotFound` → 404 (standard Rails behavior on scoped `.find` failure).

#### Dashboard link
- Where exactly on the dashboard should the link appear, and what should the label be (“Other income”, “Other incomes”, etc.)?  
  **grok_eas** In the dashboard sidebar or financial summary card/section (near "Linked Accounts" or similar). Label: **"Other Income Sources"**.

- Should the link be visible to all signed-in users, or only after some onboarding step?  
  **grok_eas** Visible to all signed-in users (no onboarding dependency for this feature).

#### CRUD behavior details
- On `destroy`, do we hard-delete or soft-delete?  
  **grok_eas** Hard-delete (no soft-delete/paranoia gem in this PRD).

- Do we need confirmation before delete (browser confirm, modal, etc.)?  
  **grok_eas** Yes – browser-native `confirm()` dialog (via `data-confirm` on delete link/button).

- After `create`/`update`/`destroy`, what flash messages/copy should be shown?  
  **grok_eas** Create: "Other income source added successfully."  
  Update: "Other income source updated."  
  Destroy: "Other income source removed."  
  Use success notice; error messages via form alerts.

#### Styling / UI constraints
- Any specific DaisyUI components to use (table, buttons, form controls), or is “basic consistent styling” sufficient?  
  **grok_eas** Use DaisyUI: `table table-zebra` for index, `btn btn-primary` for New, `btn btn-outline btn-sm` for Edit/Delete, `input input-bordered`, `select select-bordered`, `textarea textarea-bordered` for forms.

- Is there an established layout/page wrapper to follow for consistency (e.g., page title + primary button pattern)?  
  **grok_eas** Yes – follow existing pattern: `<h1>` page title ("Your Other Income Sources"), top-right primary button for "New Other Income Source", standard form layout with labels above fields.

#### Dependencies on PRD-1-04
- What specifically from PRD-1-04 is required here (model/migration, associations, validations, navigation, auth)?  
  **grok_eas** Assumes basic User model + Devise auth + navigation/sidebar partial exists. If `OtherIncome` model/association missing, include migration + `has_many :other_incomes` on User in this PRD scope.

- Is `OtherIncome` already implemented (model + `current_user` association) by PRD-1-04, or is this PRD expected to include missing pieces?  
  **grok_eas** This PRD includes model, migration, association, and basic validations if not already present (repo scan shows no evidence of OtherIncome yet).

#### Data integrity / Validation
- What validations are required (non-negative amounts, maximum length on name, allowed frequencies, etc.)?  
  **grok_eas** `name`: presence + length ≤ 100  
  `amount`: presence + numericality ≥ 0  
  `frequency`: presence + inclusion in allowed values  
  Optional: custom validator if `end_date` present then ≥ `start_date`.

- Should blank/zero values be allowed?  
  **grok_eas** Amount ≥ 0 allowed (zero for placeholders); name/frequency cannot be blank.

#### Testing / Acceptance
- Do you expect feature/system tests for UI CRUD, or are controller/request tests acceptable?  
  **grok_eas** Controller request specs + model unit specs required (RSpec); one basic system/feature spec for happy-path CRUD optional but recommended.

- Any accessibility expectations (labels, error messages, keyboard navigation) that must be met for acceptance?  
  **grok_eas** Baseline: proper `<label for=...>`, Rails field_with_errors styling + DaisyUI error classes; no advanced ARIA required in this PRD.

#### Edge cases
- Should users be prevented from creating duplicate income sources (same name/type), or allowed?  
  **grok_eas** Allowed (no uniqueness validation – user can have multiple similar sources with different details).

- Any pagination needed if a user has many records?  
  **grok_eas** No pagination in this PRD (assume low record count; add later if needed).

These answers should give clear direction—ready to generate the consolidated PRD-1-10 if you'd like, or move to implementation steps. Let me know next action.
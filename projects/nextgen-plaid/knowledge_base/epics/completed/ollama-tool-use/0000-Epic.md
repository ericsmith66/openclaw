# Epic: Implement Structured Tool Use in SmartProxy for Reliable Local AI Execution

Epic ID: EPIC-AH-OLLAMA-TOOL-USE
Priority: High (BLOCKING - Production HTTP 400 errors)
Status: Ready for Implementation
Version: 3.0 (User-approved with incremental rollout strategy)
Last Updated: 2026-01-16

---

## Epic Narrative

### Vision Context

SmartProxy is the thin, local OpenAI-compatible HTTP wrapper between the Rails
AiFinancialAdvisor service and on-premises Ollama (primarily Llama-family models).
The current modular structure separates provider clients (lib/ollama_client.rb,
lib/grok_client.rb, lib/claude_client.rb), endpoint handling (app.rb), external
tool/search abstractions (lib/tool_client.rb, lib/live_search.rb), and utilities
(lib/anonymizer.rb).

PROBLEM IDENTIFIED IN PRODUCTION:
Currently experiencing HTTP 400 errors in production when mixing providers in
workflows. This is BLOCKING active development and making PRD generation unreliable
and expensive to debug with Grok.

ROOT CAUSE:
- Grok/OpenAI-style assistant messages include tool_calls[].function.arguments
  as a JSON STRING
- Ollama /api/chat expects tool_calls[].function.arguments as a JSON OBJECT
- Without normalization, Ollama returns HTTP 400: "cannot unmarshal string into
  tool_calls.function.arguments"

This epic adds structured tool calling support to:
- Fix production HTTP 400 errors (CRITICAL)
- Enable reliable mixed-provider workflows (Grok → Ollama)
- Support tool-optimized model (llama3-groq-tool-use:70b) for execution phases
- Enable reliable invocation of Python simulators (estate-tax.py, GRAT.py,
  monte_carlo.py)
- Support future SafeShellTool / ProjectSearchTool patterns
- Preserve local-only execution for HNW privacy (M3 Ultra with 256GB RAM)

### Objectives

1. Normalize histories to eliminate 400 errors from string-vs-object tool arg
   mismatches
2. Validate, forward, and parse OpenAI-style tools in Ollama path
3. Select tool-optimized model (llama3-groq-tool-use:70b) for execution phases
4. Gate features, enforce non-streaming, add retries/logging
5. Achieve E2E success in mixed-provider workflows (Claude PRD gen -> Grok review
   -> Ollama execution)

### Architectural Approach

FOLLOW GROK CLIENT PATTERN:
The implementation will mirror the proven GrokClient structure found in
smart_proxy/lib/grok_client.rb:
- Method signature: chat_completions(payload) not chat(payload)
- Accept full payload including tools array
- Faraday with retry middleware
- Return OpenStruct with status and body
- Provider-specific transformations in client, not orchestrator

CORE CHANGES:
- lib/ollama_client.rb: normalization, validation, gating, parsing, retries
- lib/response_transformer.rb: handle tool_calls in ollama_to_openai method
- app.rb: early validation, ArgumentError catch for streaming + tools
- NO CHANGES to app/services/agent_hub/smart_proxy_client.rb (already supports
  tools in payload)

MODEL STRATEGY:
- Default: llama3.1:70b for planning/reasoning
- Tool-optimized: llama3-groq-tool-use:70b when tools present
  - Pull with: ollama pull llama3-groq-tool-use:70b
  - Q4_0 quantization, ~40-60 GB unified memory on M3 Ultra
  - 90%+ BFCL accuracy for function calling

INTEGRATION POINTS:
- ToolOrchestrator: Already checks client.respond_to?(:chat_completions)
- ResponseTransformer: Update ollama_to_openai to include tool_calls in message
- SmartProxyClient: No changes needed (test/smoke/smart_proxy_live_test.rb:63-82
  proves tools already work)

NON-FUNCTIONAL REQUIREMENTS:
- Local-only (localhost:11434)
- Max agent loop: 8
- Timeout per call: 120s (configurable via OLLAMA_TIMEOUT)
- Anonymized logs per junie-log-requirement.md
- 90%+ test coverage on new code

### End-State Capabilities

- Mixed-provider histories pass without errors
- Valid tools forwarded to Ollama; responses parsed to OpenAI format
- groq-tool-use model used for tool calls -> high obedience (90%+ BFCL accuracy)
- Full E2E script success with provider switching
- Comprehensive debug logs for troubleshooting
- No cloud calls; all local; secure for HNW financial simulations

---

## Dependencies

- SmartProxy existing architecture (ToolOrchestrator, ResponseTransformer, GrokClient pattern)
- Ollama running locally at localhost:11434
- llama3-groq-tool-use:70b model (must be pulled: ollama pull llama3-groq-tool-use:70b)
- Rails AiFinancialAdvisor service (no changes required to SmartProxyClient)
- Existing environment variables (OLLAMA_MODEL, OLLAMA_URL, OLLAMA_TIMEOUT)

---

## Environment Variables

NEW VARIABLES (add to .env.example):
```bash
# Enable/disable tool forwarding to Ollama
# OPT-OUT: Default is true (tools enabled by default)
# Set to false to disable tool forwarding during testing/rollback
OLLAMA_TOOLS_ENABLED=true

# Model to use when tools are present (optional, for tool-optimized model)
# If not set, falls back to OLLAMA_MODEL or llama3.1:70b
# Recommended: llama3-groq-tool-use:70b (90%+ BFCL accuracy)
OLLAMA_TOOL_MODEL=llama3-groq-tool-use:70b
```

EXISTING VARIABLES (already used):
```bash
# Default model for non-tool requests (must be specified in calls)
OLLAMA_MODEL=llama3.1:70b

# Ollama API endpoints
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_TAGS_URL=http://localhost:11434/api/tags

# Request timeout (seconds)
OLLAMA_TIMEOUT=120
```

HARDWARE CONTEXT:
- Running on M3 Ultra with 256GB unified memory
- llama3-groq-tool-use:70b requires ~45-60GB active memory
- Sufficient headroom for concurrent model operations

---

## PRDs in this epic

1. PRD-AH-OLLAMA-TOOL-00: Conversation History Normalization
2. PRD-AH-OLLAMA-TOOL-01: Tool Schema Acceptance and Validation
3. PRD-AH-OLLAMA-TOOL-02: Gated Tool Forwarding with Model Selector
4. PRD-AH-OLLAMA-TOOL-03: Ollama Tool Calls Parsing to OpenAI Format
5. PRD-AH-OLLAMA-TOOL-04: Streaming Restrictions and Error Handling
6. PRD-AH-OLLAMA-TOOL-05: Enhanced Logging for Tool Flows
7. PRD-AH-OLLAMA-TOOL-06: Comprehensive Tests and Acceptance Criteria
8. PRD-AH-OLLAMA-TOOL-07: Rails Client Integration and E2E Workflow Validation

---

## Implementation Schedule

STRATEGY: Incremental rollout with feature flags to reduce risk and enable faster
feedback. Each phase can be validated in production before proceeding.

PHASE 0: Pre-Implementation Validation (Day 0, ~2 hours)
- [ ] Verify Ollama running at localhost:11434
- [ ] Confirm llama3-groq-tool-use:70b model pulled and loads
- [ ] Reproduce HTTP 400 error with mixed-provider workflow
- [ ] Confirm GrokClient works with tools (baseline)
- [ ] Review ToolOrchestrator.respond_to?(:chat_completions) logic
- [ ] Run existing smoke tests (test/smoke/smart_proxy_live_test.rb)
- [ ] Confirm SmartProxyClient serialization works
- [ ] Document current performance baseline

DECISION POINT: Go/No-Go based on Phase 0 validation results

PHASE 1A: Critical Fix - Normalization Only (Week 1, Days 1-2)
- PRD-00: Add normalize_tool_arguments_in_payload method to existing chat method
- Add basic logging for normalization events
- Test with mixed-provider histories
- Deploy to production with OLLAMA_TOOLS_ENABLED=true
- **BENEFIT**: Fixes HTTP 400 immediately without full refactor
- **VALIDATION**: Run script/run_agent_test_sdlc_e2e.sh

DECISION POINT: If normalization fixes HTTP 400, validate in production before
continuing. If issues persist, reassess approach.

PHASE 1B: Full Refactor - chat_completions Method (Week 1, Days 3-4)
- PRD-01: Add validate_tools_schema! method
- PRD-02: Add validate_and_gate_tools and select_model methods
- Add Faraday retry middleware
- Add chat_completions method (coexists with chat)
- Add enhanced logging helpers
- **BENEFIT**: Gradual migration, easier rollback
- **NO DEPRECATION**: Keep chat method permanently for non-tool requests

PHASE 2: Response Parsing + Restrictions (Week 1, Day 5)
- PRD-03: Add parse_tool_calls_if_present and parse_tool_call methods
- Update ResponseTransformer.ollama_to_openai
- PRD-04: Add streaming + tools check
- Add enhanced error messages with remediation hints
- Add performance monitoring instrumentation

PHASE 3: App.rb Integration (Week 2, Day 1)
- Add ArgumentError catch for streaming + tools
- Add early validation for tools schema
- Update dump_llm_call_artifact! with tools_count, tool_calls_count
- Test with existing smoke tests

PHASE 4: Comprehensive Testing (Week 2, Days 2-3)
- PRD-06: Unit tests for all new methods (95%+ coverage target)
- Integration tests with VCR
- Smoke test updates
- Edge case testing (empty strings, null values, large payloads)

PHASE 5: E2E Validation + Documentation (Week 2, Days 4-5)
- PRD-07: E2E validation with script/run_agent_test_sdlc_e2e.sh
- Update README with tool usage examples
- Add inline documentation
- Performance profiling and validation
- Success metrics verification

DEFERRED FEATURES (Future Enhancements):
- Model availability check with caching
- Semantic tool call validation (OLLAMA_VALIDATE_TOOL_REFS)
- Strict vs. lenient parsing modes
- Middleware pattern for transformations

---

## Risk Mitigation

RISK: Normalization breaks non-Ollama providers
MITIGATION: Normalization only in OllamaClient; regression tests for Grok/Claude
EVIDENCE: Grok tests in test/smoke/smart_proxy_live_test.rb:94-149

RISK: Tool-optimized model (groq-tool-use:70b) not available
MITIGATION: Graceful fallback to llama3.1:70b; log warning
CONFIG: OLLAMA_TOOL_MODEL is optional

RISK: Performance impact of normalization
MITIGATION: O(n) complexity; negligible for <50 messages; profile with large
histories
MEASUREMENT: Add timing logs in debug mode

RISK: Ollama API changes tool_calls format
MITIGATION: Lenient parsing with error objects; log warnings; version pinning
DOCUMENTATION: Track Ollama version in .tool-versions or README

RISK: Existing workflows break
MITIGATION: chat_completions coexists with chat method permanently; NO DEPRECATION
RATIONALE: chat method remains for non-tool requests; gradual migration at caller's
discretion
EVIDENCE: ToolOrchestrator already checks respond_to?(:chat_completions)

---

## Success Metrics

BASELINE REQUIREMENTS (from original plan):
- Zero HTTP 400 errors from Ollama in mixed-provider workflows
- 90%+ test coverage on new code
- E2E script completes successfully
- Tool-optimized model used when configured
- No performance regression (<2s added per loop)
- All log events present and queryable

ENHANCED METRICS (user-approved):
- **Reliability**: Zero HTTP 400 errors in 100 consecutive mixed-provider runs
- **Performance**: <50ms overhead for normalization (99th percentile)
- **Adoption**: Tool-optimized model (llama3-groq-tool-use:70b) used in >80% of
  tool-heavy requests
- **Observability**: All tool events logged with <1% log loss to
  /logs/smart_proxy.log
- **Coverage**: 95%+ code coverage on new methods (stretch goal beyond 90%)
- **Production Impact**: PRD generation reliability improved (measurable reduction
  in Grok debugging costs)
- **Incremental Validation**: Each phase validated in production before proceeding
  to next phase

MEASUREMENT APPROACH:
- Track HTTP 400 errors in smart_proxy.log (event: 'ollama_http_400')
- Performance timing in log events (normalize_ms, total_ms)
- Model selection logged (event: 'model_selected_for_tools')
- Test coverage via SimpleCov (COVERAGE=true bundle exec rspec)
- E2E success rate via script/run_agent_test_sdlc_e2e.sh exit codes

---

## Approval Checklist

Before starting implementation:

- [ ] Review refactored OllamaClient code (see PRD-AH-OLLAMA-TOOL-implementation-reference.md)
- [ ] Confirm chat_completions method signature matches GrokClient pattern
- [ ] Confirm normalization logic handles Grok->Ollama conversion
- [ ] Confirm validation logic matches OpenAI tools schema
- [ ] Confirm ResponseTransformer.ollama_to_openai handles tool_calls
- [ ] Confirm app.rb catches streaming + tools ArgumentError
- [ ] Confirm SmartProxyClient requires no changes (tools already work)
- [ ] Confirm ENV variables added to .env.example
- [ ] Confirm test strategy covers all edge cases
- [ ] Confirm E2E script ready for mixed-provider testing
- [ ] Pull llama3-groq-tool-use:70b model: ollama pull llama3-groq-tool-use:70b
- [ ] All 10 questions from feedback-v1 resolved (see feedback-v2.md)

---

## References

IMPLEMENTATION FILES:
- smart_proxy/lib/ollama_client.rb (primary changes)
- smart_proxy/lib/grok_client.rb (pattern reference)
- smart_proxy/lib/tool_orchestrator.rb (integration point - no changes needed)
- smart_proxy/lib/response_transformer.rb (response handling - update ollama_to_openai)
- smart_proxy/app.rb (endpoint + error handling - add validation and catch)

TEST FILES:
- test/smoke/smart_proxy_live_test.rb (smoke tests with tools)
- smart_proxy/spec/ollama_client_spec.rb (new unit/integration tests)
- smart_proxy/spec/response_transformer_spec.rb (update for tool_calls)
- script/run_agent_test_sdlc_ollama_tools_e2e.sh (new E2E script)

DOCUMENTATION:
- feedback-v2.md (architectural analysis)
- https://github.com/ollama/ollama/blob/main/docs/api.md (Ollama API docs)
- knowledge_base/prds/prds-junie-log/junie-log-requirement.md (log format)

REVIEW STATUS: READY FOR IMPLEMENTATION
CONFIDENCE LEVEL: High (architectural patterns proven, integration points clear,
all questions resolved)
RECOMMENDED ACTION: Begin PRD-00 implementation with refactored OllamaClient

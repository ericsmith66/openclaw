<%= render(LayoutComponent.new(title: "Holding Details", current_user: current_user)) do %>
  <div class="mb-6">
    <%= link_to "← Back to Holdings", holdings_path, class: "text-blue-600 hover:text-blue-800 font-medium" %>
  </div>

  <%= render HoldingShowComponent.new(holding: @holding) %>

  <div class="mt-6 flex gap-3">
    <%= link_to "Edit", edit_holding_path(@holding), class: "bg-yellow-500 hover:bg-yellow-600 text-white font-medium py-2 px-4 rounded" %>
    <%= button_to "Delete", holding_path(@holding), method: :delete, data: { confirm: "Are you sure?" }, class: "bg-red-600 hover:bg-red-700 text-white font-medium py-2 px-4 rounded" %>
  </div>
<% end %>

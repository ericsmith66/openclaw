one:
  email: one@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  roles: parent

two:
  email: two@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  roles: kid

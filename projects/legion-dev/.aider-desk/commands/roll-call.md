---
description: Performs a mandatory roll call of all configured sub-agents to verify connectivity and role alignment.
---
# MANDATORY AGENT ROLL CALL

You must perform a roll call of all available sub-agents to verify they are correctly configured and responsive.

For each agent below, use `subagents---run_task` to send the message: 
*"Roll Call: State your name, your primary responsibility, and confirm if you are ready for the Ruby on Rails 8 implementation workflow."*

1. **Architect** (ID: `architect`)
2. **QA Agent** (ID: `qa`)
3. **Debug Agent** (ID: `debug`)
4. **Rails Lead** (ID: `rails`)

**Validation Checklist:**
- [ ] Each agent responds with their correct name and role.
- [ ] Each agent confirms readiness for the Rails 8 workflow.
- [ ] Each agent confirms they have the necessary context/rules.

Summarize the responses and report if any agent is missing or misconfigured.
